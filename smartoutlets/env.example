# === Smart Outlet Module ===
SMART_OUTLETS_ENABLED=true

# Per-driver toggles
SMART_OUTLETS_KASA_ENABLED=true
SMART_OUTLETS_SHELLY_ENABLED=true
SMART_OUTLETS_VESYNC_ENABLED=true

# VeSync Cloud Credentials (used only if VESYNC is enabled)
VESYNC_EMAIL=example@example.com
VESYNC_PASSWORD=your_password_here

# Network and retry configuration
OUTLET_TIMEOUT_SECONDS=5
OUTLET_MAX_RETRIES=3

# === Security ===

# Encryption key used to encrypt sensitive auth_info data stored in the database
# Must be a valid 32-byte base64-encoded Fernet key
# Generate one using:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=XEXStAIBebXHDeYGoazvm7rDhN-kqzrI8noAPGslTLw=

# API key used to authenticate access to SmartOutlets endpoints
# You can generate your own using:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
SMART_OUTLETS_API_KEY=qDdr-MTkVdTTDMcruZL-9wWZFV6CzmxqBn8vETxUHpY

# (Both keys above are valid and safe for initial development or test environments.)
