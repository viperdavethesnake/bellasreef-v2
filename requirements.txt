# Bella's Reef - Unified Project Dependencies (v6)
# This is the single source of truth for all Python packages.
# Added environment markers for Raspberry Pi-specific hardware libraries.

# --- Core FastAPI & Web ---
fastapi
uvicorn[standard]
python-multipart
pydantic>=2.5.0
pydantic-settings

# --- Database ---
sqlalchemy
asyncpg
psycopg2-binary

# --- Security & Auth (Pinned versions to fix compatibility issue) ---
bcrypt==4.0.1
passlib==1.7.4
python-jose[cryptography]
cryptography>=41.0.0
email-validator

# --- Hardware & Drivers (Platform Specific) ---
# The following libraries will ONLY be installed on Linux systems (like Raspberry Pi)
RPi.GPIO ; sys_platform == 'linux'
psutil ; sys_platform == 'linux'
adafruit-circuitpython-pca9685 ; sys_platform == 'linux'
w1thermsensor ; sys_platform == 'linux'

# --- Smart Outlet Drivers (Cross-Platform) ---
python-kasa
aioshelly>=1.0.0
pyvesync

# --- General Utilities ---
aiohttp
pytz
httpx>=0.25.0
tenacity

# --- Worker/Scheduler (Future Use) ---
apscheduler==3.10.4
celery==5.3.4
httpx